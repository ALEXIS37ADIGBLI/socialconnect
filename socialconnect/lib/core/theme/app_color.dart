import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color.fromRGBO(31, 132, 137, 0);
  static const backgroung = Color.fromRGBO(255, 255, 255, 0);
  static const text = Color.fromRGBO(0, 0, 0, 0);
}
